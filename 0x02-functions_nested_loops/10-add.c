#include "main.h"
/**
 * main - Entry Point
 * Descprition: 'adds two digits and returns the result'
 * Return Always 0 (Success)
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}

